version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "REPLACE_WITH_TASKDEF_ARN_OR_REF"
        LoadBalancerInfo:
          ContainerName: containerName
          ContainerPort: 8080
